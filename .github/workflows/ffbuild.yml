name: ffbuild

on:
  push:
    branches:
      - 'dev-htop'

jobs:
    build:
        runs-on: ubuntu-22.04
        steps:
            - name: checkout
              uses: actions/checkout@v4

            - name: build
              run: ./build.sh

            - name: upload
              uses: actions/upload-artifact@v4
              with:
                  name: htop
                  path: ./htop.tar